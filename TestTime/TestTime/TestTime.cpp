// TestTime.cpp : 定义控制台应用程序的入口点。
//

#include "stdafx.h"
#include "time.h"
#include <string>
int main()
{
	//time_t start,end;
	// time(&start);

	// time(&end);
	std::string tmp("55555");
	std::string word;
	auto tmp2=getline(tmp,)
	int st = 302;
	 int stt = st - st % 60;

    return 0;
}

